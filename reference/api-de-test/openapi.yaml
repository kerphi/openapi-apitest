openapi: 3.0.0
info:
  title: API de test
  version: '1.0'
  contact:
    name: Stéphane Gully
    email: noreply@abes.fr
  license:
    name: MIT
  description: "Ceci est une api de test permettant de tester l'outil https://stoplight.io/studio/"
servers:
  - url: 'http://localhost:3000'
    description: "Racine de l'API"
paths:
  '/users/{userid}':
    parameters:
      name: userid
      in: path
      required: true
      type: string
    get:
      summary: Your GET endpoint
      tags: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    description: "identifiant interne de l'utilisateur"
                  displayName:
                    type: string
                    description: "Le nom, prénom de l'utilisateur"
              examples:
                example-1:
                  value:
                    id: 3424
                    displayName: Stephane G
        '404':
          description: Not Found
      description: "Cette route permet de récupérer les données d'un utilisateur passé en argument."
      parameters:
        - schema:
            type: string
          in: query
          description: ''
components:
  schemas: {}
  securitySchemes:
    scheme_0:
      type: http
      scheme: Basic
